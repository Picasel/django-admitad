language: python

python:
  - "3.7-dev"  # 3.7 development branch

install:
  - pip install -r tests/requirements.txt
  - pip install codecov

script:
  - pytest --cov=./admitad

after_success:
  - codecov